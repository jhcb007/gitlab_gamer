{
  "name": "angular-seed",
  "private": true,
  "version": "0.0.0",
  "description": "A starter project for AngularJS",
  "repository": "https://github.com/angular/angular-seed",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "gulp": "~3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-concat-css": "^2.3.0",
    "gulp-cssmin": "^0.1.7",
    "gulp-htmlmin": "~3.0.0",
    "gulp-uglify": "^2.1.2",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "update-deps": "npm update",
    "postupdate-deps": "bower update",
    "prestart": "npm install",
    "start": "http-server -a 192.168.254.247 -p 8000 -c-1 ./app",
    "pretest": "npm install",
    "preupdate-webdriver": "npm install"
  },
  "dependencies": {
    "bootstrap": "^4.1.3",
    "popper.js": "^1.14.1"
  }
}
